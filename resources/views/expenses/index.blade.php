@extends('layouts.app') @section('title','Expenses') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="page-title mb-0">Expenses</h1><p class="page-subtitle">Purchases, operations, vendors, and bill records.</p></div><div class="d-flex gap-2"><button class="btn btn-light" data-bs-toggle="modal" data-bs-target="#categories"><i class="bi bi-tags me-1"></i>Categories</button><a href="{{ route('expenses.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i>Add expense</a></div></div>
<div class="card mb-4"><div class="card-body"><form class="row g-2"><div class="col-md-3"><input type="date" name="from" class="form-control" value="{{ request('from') }}"></div><div class="col-md-3"><input type="date" name="to" class="form-control" value="{{ request('to') }}"></div><div class="col-md-3"><select name="category" class="form-select"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-dark w-100">Apply filters</button></div></form></div></div>
<div class="card"><div class="table-responsive"><table class="table data-table"><thead><tr><th>Date</th><th>Category</th><th>Vendor</th><th>Notes</th><th>Bill</th><th class="text-end">Amount</th><th></th></tr></thead><tbody>@forelse($expenses as $expense)<tr><td>{{ $expense->expense_date->format('d M Y') }}</td><td><span class="badge" style="background:{{ $expense->category->color }}22;color:{{ $expense->category->color }}">{{ $expense->category->name }}</span></td><td>{{ $expense->vendor_name?:'—' }}</td><td class="text-muted">{{ str($expense->notes)->limit(50) ?: '—' }}</td><td>@if($expense->bill_path)<a href="{{ route('expenses.bill',$expense) }}" class="btn btn-sm btn-light"><i class="bi bi-paperclip"></i></a>@else—@endif</td><td class="text-end fw-bold">{{ \App\Models\Setting::value('currency','₹') }}{{ number_format($expense->amount,2) }}</td><td><div class="dropdown"><button class="btn btn-sm btn-light" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></button><div class="dropdown-menu dropdown-menu-end"><a class="dropdown-item" href="{{ route('expenses.edit',$expense) }}">Edit</a><form method="post" action="{{ route('expenses.destroy',$expense) }}" data-confirm="Archive this expense?">@csrf @method('delete')<button class="dropdown-item text-danger">Delete</button></form></div></div></td></tr>@empty<tr><td colspan="7" class="empty-state">No expenses found.</td></tr>@endforelse</tbody></table></div>@if($expenses->hasPages())<div class="card-footer bg-white">{{ $expenses->links() }}</div>@endif</div>
<div class="modal fade" id="categories"><div class="modal-dialog modal-lg"><div class="modal-content"><div class="modal-header"><h5 class="modal-title">Expense categories</h5><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><form method="post" action="{{ route('expense-categories.store') }}" class="row g-2 mb-4">@csrf<div class="col"><input name="name" class="form-control" placeholder="New category name" required></div><div class="col-auto"><input name="color" type="color" value="#F4B400" class="form-control form-control-color"></div><div class="col-auto"><button class="btn btn-primary">Add</button></div></form><div class="row g-2">@foreach($categories as $category)<div class="col-md-6"><form method="post" action="{{ route('expense-categories.update',$category) }}" class="d-flex gap-2 align-items-center border rounded-3 p-2">@csrf @method('put')<input type="color" name="color" value="{{ $category->color }}" class="form-control form-control-color"><input name="name" value="{{ $category->name }}" class="form-control"><input type="hidden" name="is_active" value="0"><div class="form-check form-switch"><input type="checkbox" name="is_active" value="1" class="form-check-input" @checked($category->is_active)></div><button class="btn btn-sm btn-light"><i class="bi bi-check-lg"></i></button></form></div>@endforeach</div></div></div></div></div>
@endsection
