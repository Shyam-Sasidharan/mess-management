<div class="row g-3"><div class="col-12"><label class="form-label">Customer subscription *</label><select name="subscription_id" class="form-select subscription-select" required><option value="">Choose customer…</option>@foreach($subscriptions as $sub)<option value="{{ $sub->id }}" data-breakfast="{{ (int)$sub->breakfast }}" data-lunch="{{ (int)$sub->lunch }}" data-dinner="{{ (int)$sub->dinner }}" @selected(old('subscription_id',$item?->subscription_id)==$sub->id)>{{ $sub->customer->name }} · {{ $sub->subscription_no }} · {{ $sub->start_date->format('d M') }}–{{ $sub->end_date->format('d M Y') }}</option>@endforeach</select></div><div class="col-12"><label class="form-label">Hold date *</label><input type="date" name="hold_date" class="form-control" value="{{ old('hold_date',$item?->hold_date?->toDateString()??today()->toDateString()) }}" required></div><div class="col-12"><label class="form-label">Meals still required on this date</label><div class="row g-2">@foreach(['breakfast'=>'Breakfast','lunch'=>'Lunch','dinner'=>'Dinner'] as $meal=>$label)<div class="col-4"><div class="form-check border rounded-3 p-3 ps-5"><input class="form-check-input" type="checkbox" name="{{ $meal }}_required" value="1" id="{{ $meal }}{{ $item?->id??'new' }}" @checked(old($meal.'_required',$item?->{$meal.'_required'}??true))><label class="form-check-label" for="{{ $meal }}{{ $item?->id??'new' }}">{{ $label }}</label></div></div>@endforeach</div><div class="form-text">Uncheck every subscribed meal for a compensated full-day hold.</div></div><div class="col-12"><label class="form-label">Reason</label><input name="reason" class="form-control" value="{{ old('reason',$item?->reason) }}" placeholder="Travel, personal request…"></div><div class="col-12"><label class="form-label">Notes</label><textarea name="notes" class="form-control" rows="3">{{ old('notes',$item?->notes) }}</textarea></div></div>
