@extends('layouts.auth') @section('title','Forgot password') @section('content')<h1 class="h3 fw-bold">Reset your password</h1><p class="text-muted mb-4">We'll email a secure reset link to your account.</p>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<form method="post" action="{{ route('password.email') }}">@csrf<label class="form-label">Email address</label><input type="email" name="email" class="form-control mb-3" value="{{ old('email') }}" required><button class="btn btn-primary w-100">Send reset link</button><a href="{{ route('login') }}" class="btn btn-link w-100 mt-2">Back to sign in</a></form>@endsection
