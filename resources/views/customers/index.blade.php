@extends('layouts.app') @section('title','Customers') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="page-title mb-0">Customers</h1><p class="page-subtitle">Subscriptions, delivery details, and customer history.</p></div><a href="{{ route('customers.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i> Add customer</a></div>
<div class="card mb-4"><div class="card-body"><form class="row g-2"><div class="col-md-5"><input name="search" class="form-control" value="{{ request('search') }}" placeholder="Search name, code, mobile or address"></div><div class="col-md-2"><select name="status" class="form-select"><option value="">All statuses</option>@foreach(['active','expired','paused'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select></div><div class="col-md-3"><select name="place" class="form-select"><option value="">All areas</option>@foreach($places as $place)<option @selected(request('place')===$place)>{{ $place }}</option>@endforeach</select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-dark flex-fill">Filter</button><a href="{{ route('customers.index') }}" class="btn btn-light text-nowrap m-auto"><i class="bi bi-x-circle"></i></a></div></form></div></div>
<div class="card"><div class="table-responsive"><table class="table data-table"><thead><tr><th>Customer</th><th>Contact</th><th>Package</th><th>Subscription</th><th>Payment</th><th>Status</th><th></th></tr></thead><tbody>@forelse($customers as $customer)@php($sub=$customer->currentSubscription)<tr><td><div class="d-flex gap-2 align-items-center"><span class="avatar">{{ strtoupper(substr($customer->name,0,1)) }}</span><div><a class="fw-bold text-dark text-decoration-none" href="{{ route('customers.show',$customer) }}">{{ $customer->name }}</a><div class="small text-muted">{{ $customer->customer_code }} · {{ $customer->place ?: 'No area' }}</div></div></div></td><td>{{ $customer->primary_mobile }}<div class="small text-muted text-truncate" style="max-width:200px">{{ $customer->primary_address }}</div></td><td>{{ $sub?->meal_names ?: '—' }}<div class="small text-muted">{{ $sub?->meal_count ? $sub->meal_count.' time package' : '' }}</div></td><td>@if($sub){{ $sub->start_date->format('d M') }} — {{ $sub->end_date->format('d M Y') }}<div class="small text-muted">{{ $sub->subscription_days }} days</div>@else—@endif</td><td>@if($sub)<span class="badge status-{{ $sub->payment_status }}">{{ ucfirst($sub->payment_status) }}</span><div class="small text-muted mt-1">Due {{ \App\Models\Setting::value('currency','₹') }}{{ number_format($sub->outstanding_amount,2) }}</div>@else—@endif</td><td><span class="badge status-{{ $customer->status }}">{{ ucfirst($customer->status) }}</span></td><td><div class="dropdown"><button class="btn btn-sm btn-light" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></button><div class="dropdown-menu dropdown-menu-end"><a class="dropdown-item" href="{{ route('customers.show',$customer) }}">View history</a><a class="dropdown-item" href="{{ route('customers.edit',$customer) }}">Edit</a><a class="dropdown-item" href="{{ route('customers.renew',$customer) }}">Renew</a></div></div></td></tr>@empty<tr><td colspan="7" class="empty-state"><i class="bi bi-people fs-1 d-block mb-2"></i>No customers match this view.</td></tr>@endforelse</tbody></table></div>@if($customers->hasPages())<div class="card-footer bg-white">{{ $customers->links() }}</div>@endif</div>
@endsection
