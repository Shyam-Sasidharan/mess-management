@extends('layouts.app') @section('title','Payments') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="page-title mb-0">Payment ledger</h1><p class="page-subtitle">Manual receipts and outstanding subscription balances.</p></div><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addPayment"><i class="bi bi-plus-lg me-1"></i>Record payment</button></div>
<div class="card mb-4"><div class="card-body"><form class="d-flex gap-2"><input name="search" class="form-control flex-grow-1" style="min-width:220px" value="{{ request('search') }}" placeholder="Search customer, mobile or code"><button class="btn btn-dark px-4">Search</button><a href="{{ route('payments.index') }}" class="btn btn-light text-nowrap m-auto"><i class="bi bi-x-circle"></i></a></form></div></div>
<div class="card"><div class="table-responsive"><table class="table data-table"><thead><tr><th>Date</th><th>Receipt</th><th>Customer</th><th>Subscription</th><th>Method</th><th class="text-end">Amount</th><th></th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->payment_date->format('d M Y') }}</td><td class="fw-semibold">{{ $payment->receipt_no }}</td><td><a href="{{ route('customers.show',$payment->customer) }}">{{ $payment->customer->name }}</a><div class="small text-muted">{{ $payment->customer->primary_mobile }}</div></td><td>{{ $payment->subscription?->subscription_no ?: '—' }}</td><td><span class="badge bg-light text-dark">{{ strtoupper($payment->method) }}</span></td><td class="text-end fw-bold">{{ \App\Models\Setting::value('currency','₹') }}{{ number_format($payment->amount,2) }}</td><td><div class="dropdown"><button class="btn btn-sm btn-light" data-bs-toggle="dropdown"><i class="bi bi-three-dots"></i></button><div class="dropdown-menu dropdown-menu-end"><button class="dropdown-item" data-bs-toggle="modal" data-bs-target="#editPayment{{ $payment->id }}">Edit</button><form method="post" action="{{ route('payments.destroy',$payment) }}" data-confirm="Delete this receipt?">@csrf @method('delete')<button class="dropdown-item text-danger">Delete</button></form></div></div><div class="modal fade" id="editPayment{{ $payment->id }}"><div class="modal-dialog"><form method="post" action="{{ route('payments.update',$payment) }}" class="modal-content">@csrf @method('put')<div class="modal-header"><h5 class="modal-title">Edit {{ $payment->receipt_no }}</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body">@include('payments._fields',['selected'=>$payment->subscription_id,'item'=>$payment])</div><div class="modal-footer"><button class="btn btn-primary">Save changes</button></div></form></div></div></td></tr>@empty<tr><td colspan="7" class="empty-state">No payments recorded yet.</td></tr>@endforelse</tbody></table></div>@if($payments->hasPages())<div class="card-footer bg-white">{{ $payments->links() }}</div>@endif</div>
<div class="modal fade" id="addPayment"><div class="modal-dialog"><form method="post" action="{{ route('payments.store') }}" class="modal-content">@csrf<div class="modal-header"><h5 class="modal-title">Record payment</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body">@include('payments._fields',['selected'=>request('subscription'),'item'=>null])</div><div class="modal-footer"><button type="button" class="btn btn-light" data-bs-dismiss="modal">Cancel</button><button class="btn btn-primary">Save receipt</button></div></form></div></div>
@endsection
