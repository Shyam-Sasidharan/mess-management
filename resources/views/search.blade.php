@extends('layouts.app') @section('title','Search') @section('content')<div class="mb-4"><h1 class="page-title mb-0">Search</h1><p class="page-subtitle">Results for “{{ $term }}”</p></div><form class="card card-body mb-4"><div class="input-group"><input name="q" value="{{ $term }}" class="form-control" placeholder="Type at least 2 characters" autofocus><button class="btn btn-primary">Search</button></div></form><div class="row g-4"><div class="col-lg-7"><div class="card"><div class="card-body"><div class="section-title mb-3">Customers ({{ $customers->count() }})</div><div class="list-group list-group-flush">@forelse($customers as $customer)<a href="{{ route('customers.show',$customer) }}" class="list-group-item list-group-item-action px-0 d-flex justify-content-between"><div><strong>{{ $customer->name }}</strong><div class="small text-muted">{{ $customer->customer_code }} · {{ $customer->primary_mobile }}</div></div><span class="badge status-{{ $customer->status }} align-self-center">{{ ucfirst($customer->status) }}</span></a>@empty<div class="empty-state">No customer matches.</div>@endforelse</div></div></div></div><div class="col-lg-5"><div class="card"><div class="card-body"><div class="section-title mb-3">Expenses ({{ $expenses->count() }})</div><div class="list-group list-group-flush">@forelse($expenses as $expense)<a href="{{ route('expenses.edit',$expense) }}" class="list-group-item list-group-item-action px-0"><div class="d-flex justify-content-between"><strong>{{ $expense->vendor_name?:$expense->category->name }}</strong><strong>{{ number_format($expense->amount,2) }}</strong></div><div class="small text-muted">{{ $expense->category->name }} · {{ $expense->expense_date->format('d M Y') }}</div></a>@empty<div class="empty-state">No expense matches.</div>@endforelse</div></div></div></div></div>@endsection
